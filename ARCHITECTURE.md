# 🎥 Stream App - Visual Architecture & Setup Guide

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    INTERNET / LAN NETWORK                        │
└─────────────────────────────────────────────────────────────────┘
         ↑                           ↑                      ↑
         │                           │                      │
         │ HTTP/HTTPS                │ HTTP/HTTPS          │ HTTP/HTTPS
         │                           │                      │
    ┌────┴────────┐            ┌────┴────────┐        ┌────┴────────┐
    │ HOST DEVICE │            │SERVER       │        │GUEST DEVICE │
    │(Desktop)    │            │(Node.js)    │        │(Mobile)     │
    │             │            │             │        │             │
    │ Browser     │◄──WebRTC──►│Socket.IO    │◄──────►│ Browser     │
    │host.html    │  P2P Media │+ Signaling  │WebSocket│guest.html   │
    │             │  Streaming │+ Sessions   │        │             │
    │ Features:   │            │             │        │ Features:   │
    │ - View      │            │ Runs on     │        │ - Send      │
    │   video     │            │ :3000       │        │   video     │
    │ - Control   │            │             │        │ - Switch    │
    │   camera    │            │             │        │   cameras   │
    └─────────────┘            └─────────────┘        └─────────────┘
```

## 🔄 Complete Connection Flow

```
STEP 1: HOST INITIALIZATION
┌──────────────────────────────────────────────┐
│ User opens: http://localhost:3000/host.html  │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Browser connects to Socket.IO server         │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Emits: socket.emit('hostJoin')               │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Server receives event in server.js           │
│ Generates: sessionId = uuid.v4().substring(0,8)    │
│ Creates: new Session(sessionId)              │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Server emits back:                           │
│ socket.emit('hostSessionCreated', { sessionId })   │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Host page displays:                          │
│ http://localhost:3000/guest.html?sessionId=h8k3j2l9   │
└──────────────────────────────────────────────┘

═══════════════════════════════════════════════

STEP 2: GUEST JOINS
┌──────────────────────────────────────────────┐
│ User clicks shared link from host            │
│ Opens: http://localhost:3000/guest.html?sessionId=h8k3j2l9   │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest page reads URL parameter:              │
│ sessionId = "h8k3j2l9"                       │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest browser connects to Socket.IO           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest page shows permission request:         │
│ "Grant Camera Access"                        │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ User clicks button                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Browser prompts for camera permission:       │
│ "website.com wants to use your camera"       │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ User grants permission                       │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ JavaScript executes:                         │
│ navigator.mediaDevices.getUserMedia({        │
│   video: { facingMode: 'user' },            │
│   audio: true                                │
│ })                                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest creates RTCPeerConnection              │
│ Adds local tracks: video + audio             │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest creates SDP Offer:                     │
│ const offer = await pc.createOffer()        │
│ await pc.setLocalDescription(offer)         │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest sends Offer to Host:                   │
│ socket.emit('guestOffer', {                 │
│   sessionId: 'h8k3j2l9',                    │
│   offer: pc.localDescription                 │
│ })                                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Server routes to Host:                       │
│ hostSocket.emit('guestOffer', { offer })    │
└──────────────────────────────────────────────┘

═══════════════════════════════════════════════

STEP 3: WEBRTC HANDSHAKE
┌──────────────────────────────────────────────┐
│ Host receives Offer                          │
│ socket.on('guestOffer', async (data) {...})  │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Host creates RTCPeerConnection               │
│ Sets remote description with Guest's offer   │
│ await pc.setRemoteDescription(offer)        │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Host creates SDP Answer:                     │
│ const answer = await pc.createAnswer()      │
│ await pc.setLocalDescription(answer)        │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Host sends Answer back to Guest:             │
│ socket.emit('hostAnswer', {                 │
│   sessionId: 'h8k3j2l9',                    │
│   answer: pc.localDescription                │
│ })                                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Server routes to Guest:                      │
│ guestSocket.emit('hostAnswer', { answer })  │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest receives Answer                        │
│ Sets remote description with Host's answer   │
│ await pc.setRemoteDescription(answer)       │
└──────────────────────────────────────────────┘

═══════════════════════════════════════════════

STEP 4: ICE CANDIDATE EXCHANGE
┌──────────────────────────────────────────────┐
│ Both peers exchange ICE Candidates           │
│ These describe possible connection routes    │
│ (via STUN servers when behind NAT)           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest:  pc.onicecandidate event fired        │
│ Sends via: socket.emit('guestIceCandidate')  │
│ ↓ Routed by Server                           │
│ Host:   pc.addIceCandidate(candidate)        │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Host:  pc.onicecandidate event fired         │
│ Sends via: socket.emit('hostIceCandidate')   │
│ ↓ Routed by Server                           │
│ Guest: pc.addIceCandidate(candidate)         │
└──────────────────────────────────────────────┘

═══════════════════════════════════════════════

STEP 5: DIRECT P2P CONNECTION
┌──────────────────────────────────────────────┐
│ Once ICE candidates established:             │
│ - Direct peer-to-peer connection formed      │
│ - Server no longer in data path              │
│ - Media flows directly between peers         │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest's video tracks transmitted             │
│ ↓                                            │
│ Host's RTCPeerConnection.ontrack event      │
│ ↓                                            │
│ video.srcObject = remoteStream               │
│ ↓                                            │
│ Video plays in <video> element               │
└──────────────────────────────────────────────┘

═══════════════════════════════════════════════

STEP 6: CAMERA SWITCHING
┌──────────────────────────────────────────────┐
│ Host clicks "Switch to Back Camera"          │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Host emits:                                  │
│ socket.emit('requestCameraSwitch', {        │
│   sessionId: 'h8k3j2l9',                    │
│   camera: 'environment'                      │
│ })                                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Server routes to Guest:                      │
│ guestSocket.emit('requestCameraSwitch', {   │
│   camera: 'environment'                      │
│ })                                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest receives request                       │
│ socket.on('requestCameraSwitch', ...)        │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest stops current camera:                  │
│ localStream.getTracks().forEach(t => t.stop()) │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Browser prompts for new camera permission    │
│ (back camera facingMode: 'environment')      │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest approves permission                    │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest gets new stream:                       │
│ navigator.mediaDevices.getUserMedia({       │
│   video: { facingMode: 'environment' },     │
│   audio: true                                │
│ })                                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ Guest replaces tracks in connection:         │
│ const sender = pc.getSenders()[videoIndex]   │
│ await sender.replaceTrack(newVideoTrack)     │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│ New video stream flows through P2P           │
│ Host receives new track                      │
│ Host sees back camera video                  │
└──────────────────────────────────────────────┘
```

## 📁 File Structure Visualization

```
GitHub Project/
│
├─ server.js
│  │
│  ├─ Express app setup
│  │  └─ Serves HTML files
│  │
│  ├─ Socket.IO connection handler
│  │  │
│  │  ├─ hostJoin event
│  │  │  └─ Creates session, sends sessionId
│  │  │
│  │  ├─ guestJoin event
│  │  │  └─ Validates sessionId, adds guest
│  │  │
│  │  ├─ Signal routing
│  │  │  ├─ guestOffer → hostAnswer
│  │  │  ├─ hostAnswer → guestAnswer
│  │  │  └─ ICE candidates (both directions)
│  │  │
│  │  └─ Camera control
│  │     └─ requestCameraSwitch routing
│  │
│  └─ Session management
│     └─ Cleanup on disconnect
│
├─ host.html
│  │
│  ├─ HTML Structure
│  │  ├─ Session panel
│  │  │  └─ Display & copy sessionId
│  │  │
│  │  ├─ Video container
│  │  │  └─ <video id="guestVideo">
│  │  │
│  │  └─ Control panel
│  │     ├─ Front camera button
│  │     └─ Back camera button
│  │
│  ├─ CSS Styling
│  │  ├─ Responsive layout
│  │  ├─ Gradient background
│  │  └─ Status indicators
│  │
│  └─ JavaScript Logic
│     ├─ Socket.IO initialization
│     ├─ WebRTC peer connection
│     ├─ Event listeners
│     └─ Camera control functions
│
├─ guest.html
│  │
│  ├─ HTML Structure
│  │  ├─ Permission prompt
│  │  │  └─ "Grant Camera Access" button
│  │  │
│  │  ├─ Video container
│  │  │  └─ <video id="myVideo"> (self-view)
│  │  │
│  │  ├─ Camera indicator
│  │  │  └─ Shows "Front" or "Back"
│  │  │
│  │  └─ Status display
│  │
│  ├─ CSS Styling
│  │  ├─ Permission prompt styling
│  │  ├─ Video display
│  │  └─ Loading animation
│  │
│  └─ JavaScript Logic
│     ├─ URL parameter parsing
│     ├─ Socket.IO connection
│     ├─ Camera permission request
│     ├─ WebRTC setup
│     ├─ Offer creation
│     └─ Camera switching handler
│
├─ package.json
│  ├─ express: Web framework
│  ├─ socket.io: WebSocket library
│  ├─ cors: Cross-origin support
│  └─ uuid: Session ID generation
│
└─ Documentation
   ├─ README.md (comprehensive guide)
   ├─ QUICKSTART.md (5-minute setup)
   ├─ IMPLEMENTATION_SUMMARY.md (overview)
   ├─ CONFIGURATION.js (options)
   ├─ FILES_CREATED.md (checklist)
   ├─ COMMANDS.bat (common commands)
   └─ ARCHITECTURE.md (this file)
```

## 🎯 Setup Checklist

```
PRE-SETUP:
☐ Node.js installed (check: node --version)
☐ npm installed (check: npm --version)
☐ Project folder exists
☐ All files downloaded/created

INSTALLATION:
☐ Open PowerShell
☐ Navigate to project folder
☐ Run: npm install
☐ Wait for dependencies (may take 1-2 min)

RUNNING:
☐ Run: npm start
☐ See "Server running on" message
☐ Port 3000 is accessible

TESTING:
☐ Open browser to http://localhost:3000/host.html
☐ See session link displayed
☐ Copy the link
☐ Open new tab and paste link
☐ Click "Grant Camera Access"
☐ Approve browser permission
☐ See your camera in guest page
☐ Click camera switch buttons
☐ See camera switching work on both sides
```

## 🔌 Network Scenarios

### Scenario 1: Local Testing (Same Computer)
```
┌─────────────────────────────────┐
│ Your Computer                   │
│                                 │
│ Tab 1: host.html               │
│ ↕ (localhost:3000)             │
│ Server (Node.js)               │
│ ↑ (localhost:3000)             │
│ Tab 2: guest.html              │
└─────────────────────────────────┘

Result: Works perfectly for testing
```

### Scenario 2: Local Network (Different Computer)
```
┌──────────────────────┐     ┌──────────────────────┐
│ Computer A           │     │ Computer B (Mobile)  │
│ (Host)               │     │ (Guest)              │
│                      │     │                      │
│ host.html            │     │ Open guest link      │
│ ↕                    │     │ ↑                    │
└──────────┬───────────┘     └──────────┬───────────┘
           │ WebRTC P2P              │
           │ via 192.168.1.x        │
           └──────────────────────────┘

Result: Works over local network (LAN)
```

### Scenario 3: Internet (Different Networks)
```
┌──────────────────────┐                ┌──────────────────────┐
│ House Network        │  INTERNET      │ Mobile Network       │
│ (Host at 192.168...) │────────────→   │ (Guest via 4G/5G)   │
│                      │  with STUN     │                      │
│ server.js running    │    Servers     │ Opens guest link     │
│ on port 3000         │ (Google STUN)  │                      │
└──────────────────────┘                └──────────────────────┘

Result: Works over internet with STUN servers
```

## 🐛 Debugging Visually

```
NOT WORKING?

Step 1: Server Running?
┌─────────────────────────┐
│ npm start               │
│ Check for: "Server      │
│ running on :3000"       │
└──────────┬──────────────┘
           │ NO? Check error message
           │ YES? Continue
           ↓

Step 2: Browser Console Errors?
┌─────────────────────────┐
│ F12 → Console tab       │
│ Check for red errors    │
└──────────┬──────────────┘
           │ NO errors? Continue
           │ Errors? Fix them
           ↓

Step 3: Camera Working?
┌─────────────────────────┐
│ Grant camera permission │
│ on guest page           │
└──────────┬──────────────┘
           │ Denied? Check browser settings
           │ Allowed? Continue
           ↓

Step 4: Video Displaying?
┌─────────────────────────┐
│ Check host page         │
│ <video> element shows   │
│ guest's camera feed     │
└──────────┬──────────────┘
           │ No video? Check browser console
           │ Video works? Test camera switch
           ↓

Step 5: Camera Switch?
┌─────────────────────────┐
│ Click camera button     │
│ on host page            │
│ Check guest permission  │
│ request appears         │
└──────────┬──────────────┘
           │ Works? SUCCESS!
           │ Doesn't work? Check server logs
           ↓

WORKING! 🎉
```

## 📈 Performance Visualization

```
LATENCY COMPARISON:

Traditional Streaming:
Client → Server → Processing → Server → Client
(150-500ms latency)

Our WebRTC Implementation:
Client ←→ Client (Direct P2P)
(30-100ms latency)

↓ Better for real-time interaction!
```

## 🔐 Security Flow

```
USER PERMISSIONS:

Step 1: User visits page
        ↓
Step 2: User clicks "Grant Camera Access"
        ↓
Step 3: Browser shows permission dialog
        ↓
Step 4: User approves (or denies)
        ↓
Step 5: If approved:
        ├─ Camera access granted
        ├─ getUserMedia() returns stream
        └─ Video can be captured
        
Step 6: If denied:
        └─ No camera access
            Error shown to user
            Must try again manually

Key Points:
✅ User always in control
✅ Browser enforces permission
✅ Can't bypass the prompt
✅ Permission per camera switch
```

---

**This visual guide helps you understand the complete flow from startup to camera switching!**
